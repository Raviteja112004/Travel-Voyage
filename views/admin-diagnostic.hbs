<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Diagnostic</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100 p-6">
    <div class="max-w-7xl mx-auto">
        <h1 class="text-2xl font-bold mb-6">Database Diagnostic</h1>
        
        <div class="mb-8">
            <h2 class="text-xl font-semibold mb-4">User Table Structure</h2>
            <div class="bg-white rounded-lg shadow overflow-hidden">
                <table class="min-w-full">
                    <thead>
                        <tr class="bg-gray-50">
                            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Field</th>
                            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Type</th>
                            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Null</th>
                            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Key</th>
                            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Default</th>
                            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Extra</th>
                        </tr>
                    </thead>
                    <tbody>
                        {{#each userStructure}}
                        <tr class="border-t">
                            <td class="px-6 py-4">{{this.Field}}</td>
                            <td class="px-6 py-4">{{this.Type}}</td>
                            <td class="px-6 py-4">{{this.Null}}</td>
                            <td class="px-6 py-4">{{this.Key}}</td>
                            <td class="px-6 py-4">{{this.Default}}</td>
                            <td class="px-6 py-4">{{this.Extra}}</td>
                        </tr>
                        {{/each}}
                    </tbody>
                </table>
            </div>
        </div>
        
        <div class="mb-8">
            <h2 class="text-xl font-semibold mb-4">User Sample Data</h2>
            <div class="bg-white rounded-lg shadow overflow-auto">
                <table class="min-w-full">
                    <thead>
                        <tr class="bg-gray-50">
                            {{#with userSample.[0]}}
                                {{#each this}}
                                <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">{{@key}}</th>
                                {{/each}}
                            {{/with}}
                        </tr>
                    </thead>
                    <tbody>
                        {{#each userSample}}
                        <tr class="border-t">
                            {{#each this}}
                            <td class="px-6 py-4">{{this}}</td>
                            {{/each}}
                        </tr>
                        {{/each}}
                    </tbody>
                </table>
            </div>
        </div>
        
        <div class="mb-8">
            <h2 class="text-xl font-semibold mb-4">Booking Table Structure</h2>
            <div class="bg-white rounded-lg shadow overflow-hidden">
                <table class="min-w-full">
                    <thead>
                        <tr class="bg-gray-50">
                            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Field</th>
                            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Type</th>
                            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Null</th>
                            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Key</th>
                            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Default</th>
                            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Extra</th>
                        </tr>
                    </thead>
                    <tbody>
                        {{#each bookingStructure}}
                        <tr class="border-t">
                            <td class="px-6 py-4">{{this.Field}}</td>
                            <td class="px-6 py-4">{{this.Type}}</td>
                            <td class="px-6 py-4">{{this.Null}}</td>
                            <td class="px-6 py-4">{{this.Key}}</td>
                            <td class="px-6 py-4">{{this.Default}}</td>
                            <td class="px-6 py-4">{{this.Extra}}</td>
                        </tr>
                        {{/each}}
                    </tbody>
                </table>
            </div>
        </div>
        
        <div class="mb-8">
            <h2 class="text-xl font-semibold mb-4">Booking Sample Data</h2>
            <div class="bg-white rounded-lg shadow overflow-auto">
                <table class="min-w-full">
                    <thead>
                        <tr class="bg-gray-50">
                            {{#with bookingSample.[0]}}
                                {{#each this}}
                                <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">{{@key}}</th>
                                {{/each}}
                            {{/with}}
                        </tr>
                    </thead>
                    <tbody>
                        {{#each bookingSample}}
                        <tr class="border-t">
                            {{#each this}}
                            <td class="px-6 py-4">{{this}}</td>
                            {{/each}}
                        </tr>
                        {{/each}}
                    </tbody>
                </table>
            </div>
        </div>
        
        <div class="text-center">
            <a href="/admin/dashboard" class="px-4 py-2 bg-blue-600 text-white rounded">Back to Dashboard</a>
        </div>
    </div>
</body>
</html> 